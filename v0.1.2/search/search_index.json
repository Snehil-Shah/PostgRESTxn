{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"PostgRESTxn","text":"<p>The missing <code>/txn</code> endpoint for PostgREST.</p> <p>PostgRESTxn exposes a single HTTP endpoint that can run multiple CRUD operations against Postgres in a single, atomic, RLS-aware transaction using a highly expressive request DSL supporting cross-operation references and ORM-like JSON filters.</p>"},{"location":"#features","title":"Features","text":"<p>An overview of the batteries included (for now):</p> <ul> <li>RBAC: Uses RLS-aware access control using Postgres roles and JWT authentication (the same architecture as PostgREST).</li> <li>Atomicity: Your CRUD requests are executed as a transaction.</li> <li>Expressive DSL: Supports cross-operation references and a diverse set of operators and filters.</li> <li>JWKS &amp; OIDC: Supports fetching public keys from JWKS endpoints.</li> <li>Secure: You define the anon behavior and the schemas to expose.</li> <li>Fully stateless: Horizontally scale as you wish.</li> <li>Observability: An Admin API serving various Prometheus metrics around performance.</li> </ul>"},{"location":"#whats-next","title":"What's next","text":"<ul> <li>Get started - install and run your first transaction</li> <li>The DSL - operations, filter language, and cross-op references</li> <li>The API reference - full request and response shapes</li> <li>Production concerns - Authentication, Configuration, Observability, and Deployment</li> </ul>"},{"location":"configuration/","title":"Configuration","text":"<p>PostgRESTxn is configured entirely via environment variables.</p> <p>Tip</p> <p>To inspect the effective configuration of a running instance, hit the admin API's <code>GET /config</code> endpoint.</p>"},{"location":"configuration/#database","title":"Database","text":"Env var Default Description <code>DATABASE_URL</code> (required) Postgres connection URI: <code>postgres://user:pass@host:5432/dbname</code>. <code>POOL_SIZE</code> <code>10</code> Number of connections in the Postgrex pool. Caps your request concurrency - each in-flight transaction holds one connection for its entire lifespan. <code>DB_STATEMENT_TIMEOUT_MS</code> <code>15000</code> Per-transaction <code>statement_timeout</code> in milliseconds. Long queries are cancelled and surface as <code>query_canceled</code> (HTTP 408). <code>DB_SCHEMAS</code> <code>public</code> Comma-separated list of schemas PostgRESTxn is allowed to access. Schema-qualified table names outside this list are rejected with <code>schema_forbidden</code> (HTTP 403)."},{"location":"configuration/#authentication","title":"Authentication","text":"<p>At least one of <code>JWT_SECRET</code>, <code>JWT_JWKS_URL</code>, <code>JWT_OIDC_ISSUER</code>, or <code>ANON_ROLE</code> must be configured - PostgRESTxn refuses to boot without an auth source. The three JWT sources are mutually exclusive, pick only one.</p> Env var Default Description <code>ANON_ROLE</code> (none) Postgres role used when no <code>Authorization</code> header is provided. Without it, anonymous requests are rejected with <code>auth_required</code> (HTTP 401). See Authentication &gt; Anonymous. <code>JWT_SECRET</code> (none) HS256 secret for static-key JWT verification. See Authentication &gt; Static secret. <code>JWT_ALGO</code> <code>HS256</code> (with <code>JWT_SECRET</code>) / unset (with JWKS) JWT signing algorithm. Required with a static secret; optional override with JWKS (defaults to each key's declared <code>alg</code>). <code>JWT_JWKS_URL</code> (none) URL of a JWKS endpoint serving public keys. See Authentication &gt; JWKS. <code>JWT_OIDC_ISSUER</code> (none) OIDC issuer base URL. The JWKS endpoint is discovered at startup. See Authentication &gt; JWKS / OIDC. <code>JWT_JWKS_POLL_INTERVAL_MS</code> <code>60000</code> How often the JWKS cache refreshes, in milliseconds. Only relevant when <code>JWT_JWKS_URL</code> or <code>JWT_OIDC_ISSUER</code> is set. <code>JWT_ROLE_CLAIM_KEY</code> <code>.role</code> JSON path into the JWT claims naming the Postgres role to switch to. See Authentication &gt; The role claim. <code>JWT_AUD</code> (none) Expected <code>aud</code> claim value. Unset means audience is not enforced. <p>For the full RBAC + RLS model and a guide for each flow, see the Authentication section.</p>"},{"location":"configuration/#http","title":"HTTP","text":"Env var Default Description <code>HTTP_PORT</code> <code>4000</code> Main API server port. <code>ADMIN_HTTP_PORT</code> <code>9568</code> Admin server port. See Observability."},{"location":"deployment/","title":"Deployment","text":"<p>PostgRESTxn ships as an OCI image. Reference the image URL in your deployment config and provide the required env vars - see Configuration.</p>"},{"location":"deployment/#image","title":"Image","text":"Registry URL GitHub Container Registry <code>ghcr.io/snehil-shah/postgrestxn:&lt;tag&gt;</code> Docker Hub <code>docker.io/snehilshah/postgrestxn:&lt;tag&gt;</code> <p>Tip</p> <p>Use a tagged version (e.g., <code>:v0.1.0</code>) in production, not <code>:latest</code>.</p>"},{"location":"deployment/#ports","title":"Ports","text":"<ul> <li><code>4000</code> (default <code>HTTP_PORT</code>) - main API</li> <li><code>9568</code> (default <code>ADMIN_HTTP_PORT</code>) - admin endpoints. Restrict to your monitoring network, do not expose publicly</li> </ul>"},{"location":"deployment/#kubernetes-example","title":"Kubernetes example","text":"<p>A minimal Deployment + Service example:</p> <pre><code>apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: postgrestxn\nspec:\n  replicas: 2\n  selector:\n    matchLabels: {app: postgrestxn}\n  template:\n    metadata:\n      labels: {app: postgrestxn}\n    spec:\n      containers:\n        - name: postgrestxn\n          image: ghcr.io/snehil-shah/postgrestxn:v0.1.0\n          ports:\n            - {name: api, containerPort: 4000}\n            - {name: admin, containerPort: 9568}\n          env:\n            - name: DATABASE_URL\n              valueFrom: {secretKeyRef: {name: postgrestxn-db, key: url}}\n            - name: ANON_ROLE\n              value: web_anon\n          livenessProbe:\n            httpGet: {path: /live, port: admin}\n          readinessProbe:\n            httpGet: {path: /ready, port: admin}\n---\napiVersion: v1\nkind: Service\nmetadata:\n  name: postgrestxn\nspec:\n  selector: {app: postgrestxn}\n  ports:\n    - {name: api, port: 4000}\n    - {name: admin, port: 9568}\n</code></pre> <p>The <code>admin</code> port stays on the cluster-internal Service, don't include it in your public ingress.</p>"},{"location":"getting-started/","title":"Getting started","text":"<p>Get PostgRESTxn running against your own Postgres and execute your first transaction.</p>"},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>A running Postgres instance (14+)</li> <li>Docker</li> </ul>"},{"location":"getting-started/#1-set-up-a-postgres-role-and-table","title":"1. Set up a Postgres role and table","text":"<p>Create a role for unauthenticated requests and a sample table to operate on:</p> <pre><code>CREATE ROLE web_anon NOLOGIN;\n\nCREATE TABLE users (\n  id SERIAL PRIMARY KEY,\n  email TEXT UNIQUE NOT NULL,\n  age INT,\n  role TEXT\n);\n\nGRANT SELECT, INSERT, UPDATE, DELETE ON users TO web_anon;\nGRANT USAGE, SELECT ON SEQUENCE users_id_seq TO web_anon;\n</code></pre> <p>For setups using JWT-based authentication instead of (or alongside) anonymous access, see the Authentication section.</p>"},{"location":"getting-started/#2-run-postgrestxn","title":"2. Run PostgRESTxn","text":"<pre><code>docker run --rm -p 4000:4000 \\\n  -e DATABASE_URL=\"postgres://user:pass@host:5432/mydb\" \\\n  -e ANON_ROLE=\"web_anon\" \\\n  snehilshah/postgrestxn:latest\n</code></pre> <p>PostgRESTxn boots and listens on <code>:4000</code>. An admin server also starts on <code>:9568</code> exposing Prometheus metrics and probes, see Observability for details.</p>"},{"location":"getting-started/#3-send-a-transaction","title":"3. Send a transaction","text":"<p>POST a JSON array of operations. The example below inserts a user, then promotes them to admin if they qualify:</p> <pre><code>curl -X POST http://localhost:4000/ \\\n  -H \"Content-Type: application/json\" \\\n  -d '[\n    {\n      \"id\": \"create\",\n      \"op\": \"insert\",\n      \"table\": \"users\",\n      \"values\": [{\"email\": \"alice@example.com\", \"age\": 30}]\n    },\n    {\n      \"id\": \"promote\",\n      \"op\": \"update\",\n      \"table\": \"users\",\n      \"set\": {\"role\": \"admin\"},\n      \"where\": {\"id\": {\"eq\": \"$create.0.id\"}, \"age\": {\"gte\": 18}}\n    }\n  ]'\n</code></pre> <p>You'll get back the affected rows keyed by op id:</p> <pre><code>{\n  \"error\": null,\n  \"data\": {\n    \"create\": [{\"id\": 1, \"email\": \"alice@example.com\", \"age\": 30, \"role\": null}],\n    \"promote\": [{\"id\": 1, \"email\": \"alice@example.com\", \"age\": 30, \"role\": \"admin\"}]\n  }\n}\n</code></pre> <p>If any op had failed, the entire transaction would have rolled back. That's the atomicity guarantee.</p>"},{"location":"getting-started/#whats-next","title":"What's next","text":"<ul> <li>The request format covers the full op grammar</li> <li>The filter language lists every operator</li> <li>Cross-op references explain the <code>$create.0.id</code> syntax</li> <li>For production deployments, see Deployment</li> </ul>"},{"location":"observability/","title":"Observability","text":"<p>PostgRESTxn exposes a separate admin HTTP server (port set via <code>ADMIN_HTTP_PORT</code>, default <code>9568</code>) with the following endpoints:</p> Endpoint Purpose <code>GET /metrics</code> Prometheus text format - request and op-level metrics <code>GET /live</code> Liveness probe - returns 200 if the process is up <code>GET /ready</code> Readiness probe - returns 200 if the DB pool is reachable, 503 otherwise <code>GET /config</code> Runtime config dump as JSON (secrets redacted) <p>Warning</p> <p>Do not expose the admin port to the public internet.</p>"},{"location":"observability/#metrics","title":"Metrics","text":"Metric Type Tags <code>postgrestxn_txn_count</code> counter <code>role</code>, <code>outcome</code> <code>postgrestxn_txn_duration_milliseconds</code> histogram <code>role</code>, <code>outcome</code> <code>postgrestxn_op_count</code> counter <code>op</code>, <code>outcome</code> <code>postgrestxn_op_duration_milliseconds</code> histogram <code>op</code>, <code>outcome</code> <code>postgrestxn_op_exceptions</code> counter <code>op</code>, <code>table</code> <p><code>outcome</code> is <code>ok</code> or <code>error</code>. Histogram buckets are in milliseconds.</p>"},{"location":"observability/#request-tracing","title":"Request tracing","text":"<p>Each response carries an <code>X-Request-Id</code> header identifying the request. Clients can send their own <code>X-Request-Id</code> to propagate trace context across services; PostgRESTxn generates a UUID when one isn't provided.</p> <p>The same id is included in server logs for every line related to that request.</p>"},{"location":"api-reference/request/","title":"Request","text":"<p>PostgRESTxn's primary endpoint is <code>POST /</code>:</p> <pre><code>POST /\nContent-Type: application/json\nAuthorization: Bearer &lt;JWT&gt;\n</code></pre> <p>A <code>GET /health</code> endpoint is also exposed - see the Health check section below.</p>"},{"location":"api-reference/request/#body-shape","title":"Body shape","text":"<p>The body is a JSON array of operations. Operations execute in order in one atomic transaction.</p> <pre><code>[\n  {\"id\": \"op_a\", \"op\": \"insert\", \"table\": \"users\", \"values\": [...]},\n  {\"id\": \"op_b\", \"op\": \"select\", \"table\": \"users\", \"where\": {...}}\n]\n</code></pre>"},{"location":"api-reference/request/#op-fields","title":"Op fields","text":"<p>Every op has the same three common fields:</p> Field Type Required Description <code>id</code> string yes Op identifier (letters, digits, underscores; not starting with digits). Used as the response key and as the target of cross-op references. <code>op</code> string yes One of <code>\"insert\"</code>, <code>\"update\"</code>, <code>\"delete\"</code>, <code>\"select\"</code>. <code>table</code> string yes Table or view name. Schema-qualified names (<code>auth.users</code>) are supported when the schema is in the allowlist. <p>Per-op-type fields:</p> Op type <code>values</code> <code>set</code> <code>where</code> <code>insert</code> required - - <code>update</code> - required required <code>delete</code> - - required <code>select</code> - - optional <p>For the semantics of each field - what <code>values</code> accepts, how <code>where</code> is parsed, how references resolve - see Operations, Filters, and References.</p>"},{"location":"api-reference/request/#headers","title":"Headers","text":"Header Required Notes <code>Content-Type: application/json</code> yes The only supported content type. Other types raise <code>415 Unsupported Media Type</code>. <code>Authorization: Bearer &lt;JWT&gt;</code> conditional Required if no anonymous role (<code>ANON_ROLE</code>) is configured. See Authentication."},{"location":"api-reference/request/#request-level-errors","title":"Request-level errors","text":"<p>These are caught before any per-op validation runs. They return the <code>request_error</code> envelope with the error object.</p> Code HTTP When it fires <code>body_invalid</code> 400 Body isn't a JSON array. <code>body_empty</code> 400 Body is an empty array. <code>id_missing</code> 400 An op is missing the <code>id</code> field, or it isn't a string. <code>id_duplicate</code> 400 Two or more ops share the same <code>id</code>. <code>auth_required</code> 401 No JWT was provided and no anonymous role is configured. See Authentication. <p>Example response:</p> <pre><code>{\n  \"error\": \"request_error\",\n  \"data\": {\"code\": \"id_duplicate\", \"detail\": \"op ids must be unique\"}\n}\n</code></pre> <p>For per-op validation errors (shape, references, schema allowlist) and runtime errors (session setup, SQL execution), see Response.</p>"},{"location":"api-reference/request/#health-check","title":"Health check","text":"<pre><code>GET /health\n</code></pre> <p>Returns <code>200 {\"ok\": true}</code> if the database pool is reachable, or <code>503 {\"ok\": false, \"error\": \"database\"}</code> otherwise. No authentication required.</p> <p>For separate liveness/readiness probes plus metrics, see the admin endpoints.</p>"},{"location":"api-reference/response/","title":"Response","text":"<p>Every PostgRESTxn response carries an <code>error</code> field at the top level. <code>null</code> means success; any other value names the error category, and the <code>data</code> field carries the details.</p>"},{"location":"api-reference/response/#success","title":"Success","text":"<p>HTTP 200 with the per-op results:</p> <pre><code>{\n  \"error\": null,\n  \"data\": {\n    \"create\": [{\"id\": 1, \"email\": \"alice@example.com\"}],\n    \"fetch\": [{\"id\": 1, \"email\": \"alice@example.com\"}]\n  }\n}\n</code></pre> <ul> <li><code>error</code> is <code>null</code></li> <li><code>data</code> is a map keyed by each op's <code>id</code>; each entry is an array of affected rows</li> </ul> <p>For details on result shape per op type, see Operations &gt; Result shape.</p>"},{"location":"api-reference/response/#error-envelopes","title":"Error envelopes","text":"<p>Four distinct error envelopes, distinguished by the <code>error</code> field. They differ in shape because the cause and granularity differ.</p>"},{"location":"api-reference/response/#request_error","title":"<code>request_error</code>","text":"<p>Issues with the request as a whole: body shape, op ids, or authentication. <code>data</code> is a single error object.</p> <pre><code>{\n  \"error\": \"request_error\",\n  \"data\": {\"code\": \"id_duplicate\", \"detail\": \"op ids must be unique\"}\n}\n</code></pre> <p>HTTP status: 400 (most codes) or 401 (<code>auth_required</code>). The full code catalog lives in Request &gt; Request-level errors.</p>"},{"location":"api-reference/response/#validation_error","title":"<code>validation_error</code>","text":"<p>Per-op shape and policy failures: missing fields, malformed filters, unknown refs, forbidden schemas. <code>data</code> is a map keyed by op id where each op carries a list of error objects.</p> <pre><code>{\n  \"error\": \"validation_error\",\n  \"data\": {\n    \"op_a\": [\n      {\"code\": \"ref_unknown\", \"path\": [\"values\", 0, \"user_id\"], \"input\": \"$nope.0.id\", \"detail\": \"...\"},\n      {\"code\": \"ref_unknown\", \"path\": [\"values\", 0, \"owner_id\"], \"input\": \"$missing.0.id\", \"detail\": \"...\"}\n    ],\n    \"op_b\": [{\"code\": \"shape_invalid\", \"path\": [\"where\"], \"input\": null, \"detail\": \"...\"}]\n  }\n}\n</code></pre> <p><code>op_a</code> has two errors (both refs pointing at non-existent ops), while <code>op_b</code> has one - validation errors accumulate per op and per request.</p> <p>HTTP status: 422 by default; 403 for <code>schema_forbidden</code>.</p> <p>Specific codes are documented where they apply:</p> <ul> <li>Shape, per-op-field, and schema errors: Operations</li> <li>Filter shape errors: Filters</li> <li>Reference errors: References</li> </ul> <p>Each error object has <code>code</code>, <code>path</code> (segments into the op pointing at the offending value), <code>input</code> (the offending value itself), and <code>detail</code> (a human-readable explanation).</p>"},{"location":"api-reference/response/#session_error","title":"<code>session_error</code>","text":"<p>Transaction setup failed. <code>data</code> is a single error object because session setup is request-level, not op-level.</p> <pre><code>{\n  \"error\": \"session_error\",\n  \"data\": {\"code\": \"invalid_role\", \"detail\": \"role \\\"webanon\\\" is not a valid Postgres identifier\"}\n}\n</code></pre> <p>HTTP status is mapped from the underlying error. Common codes:</p> <ul> <li><code>invalid_role</code> (401) - configured role doesn't pass identifier validation</li> <li><code>insufficient_privilege</code> (403) - role exists but can't <code>SET LOCAL</code> for some reason</li> <li><code>undefined_object</code> (404) - role doesn't exist in Postgres</li> </ul> <p>For role configuration and the underlying RBAC model, see Authentication.</p>"},{"location":"api-reference/response/#execution_error","title":"<code>execution_error</code>","text":"<p>A specific op's SQL failed at runtime. <code>data</code> is keyed by the failing op's id with a single error object as the value.</p> <pre><code>{\n  \"error\": \"execution_error\",\n  \"data\": {\n    \"create_order\": {\n      \"code\": \"foreign_key_violation\",\n      \"detail\": \"Key (user_id)=(42) is not present in table \\\"users\\\".\",\n      \"path\": [],\n      \"input\": null\n    }\n  }\n}\n</code></pre> <p>HTTP status is mapped from the SQL error. See the table below.</p>"},{"location":"api-reference/response/#sql-error-to-http-mapping","title":"SQL Error to HTTP mapping","text":"<p>PostgRESTxn translates Postgres error codes to HTTP statuses.</p> Postgres condition HTTP Notes <code>undefined_table</code>, <code>undefined_column</code>, <code>undefined_function</code> 404 The named thing doesn't exist. <code>integrity_constraint_violation</code>, <code>unique_violation</code>, <code>foreign_key_violation</code>, <code>not_null_violation</code>, <code>check_violation</code>, <code>restrict_violation</code>, <code>exclusion_violation</code> 409 Integrity constraint conflicts. <code>deadlock_detected</code>, <code>serialization_failure</code> 409 Retryable transaction conflicts. PostgREST returns 500 here; we return 409 so clients have a meaningful \"retry\" signal. <code>insufficient_privilege</code>, <code>invalid_authorization_specification</code>, <code>invalid_password</code>, <code>invalid_grantor</code>, <code>invalid_grant_operation</code>, <code>invalid_role_specification</code>, <code>diagnostics_exception</code> 403 Permission and authorization Postgres errors. <code>query_canceled</code> 408 Statement timeout reached. PostgREST returns 500 here; we return 408 since timeouts are client-visible policy, not server bugs. <code>read_only_sql_transaction</code> 405 Tried to write through a role with read-only access. <code>data_exception</code>, <code>string_data_right_truncation</code>, <code>numeric_value_out_of_range</code>, <code>invalid_datetime_format</code>, <code>datetime_field_overflow</code>, <code>invalid_parameter_value</code>, <code>invalid_text_representation</code>, <code>division_by_zero</code> 400 Bad input values. <code>raise_exception</code> 400 <code>RAISE</code> from a PL/pgSQL function (default). <code>connection_exception</code>, <code>connection_does_not_exist</code>, <code>connection_failure</code>, <code>sqlclient_unable_to_establish_sqlconnection</code>, <code>sqlserver_rejected_establishment_of_sqlconnection</code>, <code>transaction_resolution_unknown</code>, <code>protocol_violation</code>, <code>insufficient_resources</code>, <code>disk_full</code>, <code>out_of_memory</code>, <code>too_many_connections</code> 503 Infrastructure-level failures. <code>configuration_limit_exceeded</code> 500 Operator-side configuration limit hit. <code>invalid_role</code> 401 PostgRESTxn's own code - the configured role string didn't pass identifier validation before being used in <code>SET LOCAL</code>. Anything unmapped 500 Logged at full detail server-side."},{"location":"authentication/anonymous/","title":"Anonymous access","text":"<p>Configures a Postgres role used for unauthenticated requests - requests without an <code>Authorization: Bearer ...</code> header. Useful for public-read APIs, or as a fallback role alongside JWT-based authentication.</p>"},{"location":"authentication/anonymous/#setup","title":"Setup","text":"<pre><code>ANON_ROLE=web_anon\n</code></pre> <p>On the Postgres side:</p> <pre><code>CREATE ROLE web_anon NOLOGIN;\nGRANT SELECT ON public.posts TO web_anon;\n</code></pre> <p>That's the full setup.</p>"},{"location":"authentication/anonymous/#how-it-interacts-with-jwt-auth","title":"How it interacts with JWT auth","text":"<p><code>ANON_ROLE</code> can coexist with one of <code>JWT_SECRET</code>, <code>JWT_JWKS_URL</code>, or <code>JWT_OIDC_ISSUER</code>. When both are configured:</p> <ul> <li>Request without an <code>Authorization</code> header: uses <code>ANON_ROLE</code></li> <li>Request with <code>Authorization: Bearer ...</code>: JWT is verified, role taken from claims</li> </ul> <p>This is the typical pattern for APIs that mix public reads with authenticated writes.</p>"},{"location":"authentication/anonymous/#without-anon_role","title":"Without <code>ANON_ROLE</code>","text":"<p>If <code>ANON_ROLE</code> isn't set and JWT auth is configured, anonymous requests are rejected with <code>auth_required</code> (HTTP 401). Every request must carry a Bearer token.</p>"},{"location":"authentication/jwks/","title":"JWT with JWKS / OIDC","text":"<p>Verifies JWTs against asymmetric keys fetched from a JWKS endpoint. Configure in one of two ways: point at the JWKS URL directly, or have PostgRESTxn discover it from an OIDC issuer.</p>"},{"location":"authentication/jwks/#jwks-url","title":"JWKS URL","text":"<pre><code>JWT_JWKS_URL=https://your-tenant.auth0.com/.well-known/jwks.json\n</code></pre>"},{"location":"authentication/jwks/#oidc-issuer","title":"OIDC issuer","text":"<pre><code>JWT_OIDC_ISSUER=https://accounts.google.com\n</code></pre> <p>The JWKS URL is discovered from <code>&lt;issuer&gt;/.well-known/openid-configuration</code> at startup.</p>"},{"location":"authentication/jwks/#refresh-interval","title":"Refresh interval","text":"<pre><code>JWT_JWKS_POLL_INTERVAL_MS=60000   # default\n</code></pre>"},{"location":"authentication/jwks/#algorithm-override","title":"Algorithm override","text":"<p><code>JWT_ALGO</code> is optional - by default each key's declared <code>alg</code> is honored. Set it to enforce a single algorithm across all keys.</p>"},{"location":"authentication/jwks/#audience-check","title":"Audience check","text":"<p><code>JWT_AUD</code> (optional) enforces the token's <code>aud</code> claim.</p>"},{"location":"authentication/overview/","title":"Overview","text":"<p>PostgRESTxn mirrors PostgREST's auth model directly. The entire security boundary is the Postgres role plus RLS policies on your tables.</p> <p>If you already run PostgREST, the mental model carries over verbatim: pick a role, write RLS policies, hand out JWTs that name that role.</p>"},{"location":"authentication/overview/#what-happens-on-every-request","title":"What happens on every request","text":"<p>Each request resolves to a Postgres role, and all SQL runs as that role.</p> <p>For RLS policies that need to know who the request is (not just what role), read the JWT claims from <code>current_setting('request.jwt.claims', true)::json</code>:</p> <pre><code>CREATE POLICY only_own_rows ON posts\n  FOR SELECT\n  USING (author_id = (current_setting('request.jwt.claims', true)::json -&gt;&gt; 'sub'));\n</code></pre>"},{"location":"authentication/overview/#resolution-at-request-time","title":"Resolution at request time","text":"<p>For each incoming request:</p> <ol> <li><code>Authorization: Bearer &lt;jwt&gt;</code> present \u2192 verify the JWT against the configured source (static secret, JWKS, or OIDC), extract the role from the configured claim, use it.</li> <li>No <code>Authorization</code> header \u2192 use <code>ANON_ROLE</code> if configured, otherwise reject with <code>auth_required</code> (HTTP 401).</li> </ol> <p>JWT verification failures (bad signature, expired, missing role claim, etc.) also return 401 in the <code>request_error</code> envelope.</p>"},{"location":"authentication/overview/#the-four-configuration-paths","title":"The four configuration paths","text":"<p>PostgRESTxn supports four authentication sources, configured via env vars (see Configuration):</p> Source Env var When to use Anonymous only <code>ANON_ROLE</code> For public-read APIs. This role is used if JWT is missing. JWT static secret <code>JWT_SECRET</code> (+ <code>JWT_ALGO</code>) Small services, single issuer. JWT JWKS endpoint <code>JWT_JWKS_URL</code> External IdP serves public keys at a known URL. JWT OIDC discovery <code>JWT_OIDC_ISSUER</code> OIDC-compliant IdP. PostgRESTxn discovers the JWKS endpoint automatically. <p>The three JWT sources are mutually exclusive. <code>ANON_ROLE</code> can be combined with any one of them, it provides a fallback for unauthenticated requests when JWT auth is otherwise configured.</p>"},{"location":"authentication/overview/#the-role-claim","title":"The role claim","text":"<p>For JWT flows, the claim that names the Postgres role is configurable (see Configuration) via <code>JWT_ROLE_CLAIM_KEY</code> (default: <code>.role</code>). Use a dotted path for nested claims:</p> Claim key Lookup <code>.role</code> (default) top-level <code>role</code> field <code>.app_metadata.role</code> nested - <code>{\"app_metadata\": {\"role\": \"...\"}}</code> <p>The role must be a valid Postgres identifier; otherwise the request is rejected with <code>invalid_role</code> (401).</p>"},{"location":"authentication/overview/#what-you-set-up-on-the-postgres-side","title":"What you set up on the Postgres side","text":"<p>Independent of which flow you pick, you need:</p> <pre><code>-- One role per persona the API accepts.\nCREATE ROLE web_anon NOLOGIN;   -- anonymous\nCREATE ROLE web_user NOLOGIN;   -- authenticated user\nCREATE ROLE web_admin NOLOGIN;  -- elevated\n\n-- Grants per role.\nGRANT SELECT ON public.posts TO web_anon;\nGRANT SELECT, INSERT, UPDATE, DELETE ON public.posts TO web_user;\nGRANT ALL ON public.posts TO web_admin;\n\n-- RLS policies (optional but typical).\nALTER TABLE posts ENABLE ROW LEVEL SECURITY;\nCREATE POLICY own_posts ON posts\n  USING (author_id = current_setting('request.jwt.claims', true)::json -&gt;&gt; 'sub');\n</code></pre>"},{"location":"authentication/static-secret/","title":"JWT with a secret","text":"<p>Verifies JWTs against a statically configured key. This is the simplest JWT flow as there is no JWKS server, no runtime key fetching.</p> <p>Both symmetric (HMAC) and asymmetric (RSA, ECDSA) algorithms are supported:</p> <code>JWT_ALGO</code> <code>JWT_SECRET</code> contains <code>HS256</code>, <code>HS384</code>, <code>HS512</code> A shared secret (raw string) <code>RS256</code>, <code>RS384</code>, <code>RS512</code> A PEM-encoded RSA public key <code>ES256</code>, <code>ES384</code>, <code>ES512</code> A PEM-encoded ECDSA public key"},{"location":"authentication/static-secret/#setup","title":"Setup","text":"<p>Symmetric (HS*):</p> <pre><code>JWT_SECRET=your-secret-here-at-least-32-bytes-recommended-for-hs256\nJWT_ALGO=HS256\n</code></pre> <p>Asymmetric (RS*, ES*): put the PEM-encoded public key directly in <code>JWT_SECRET</code>:</p> <pre><code>JWT_SECRET=\"-----BEGIN PUBLIC KEY-----\\nMFkwEwYHKoZIzj0CAQYIKoZ...\\n-----END PUBLIC KEY-----\"\nJWT_ALGO=ES256\n</code></pre> <p><code>JWT_ALGO</code> defaults to <code>HS256</code> when <code>JWT_SECRET</code> is set.</p> <p>PostgRESTxn validates every Bearer token against the configured key on each request.</p>"},{"location":"authentication/static-secret/#token-shape","title":"Token shape","text":"<p>PostgRESTxn expects a standard JWT. The claim containing the role is configurable via <code>JWT_ROLE_CLAIM_KEY</code> (default <code>.role</code>):</p> <pre><code>{\n  \"role\": \"web_user\",\n  \"sub\": \"alice@example.com\",\n  \"exp\": 1735689600,\n  \"iat\": 1735603200\n}\n</code></pre>"},{"location":"authentication/static-secret/#issuing-tokens","title":"Issuing tokens","text":"<p>For HMAC algorithms (HS*), the signing party uses the same shared secret you configured in <code>JWT_SECRET</code>.</p> <p>For asymmetric algorithms (RS*, ES*), the signing party uses the private key of the keypair while PostgRESTxn verifies with the corresponding public key in <code>JWT_SECRET</code>.</p> <p>Either way, any token signed with the matching key and a valid <code>exp</code> is accepted.</p>"},{"location":"authentication/static-secret/#optional-audience-check","title":"Optional audience check","text":"<p>If you set <code>JWT_AUD</code>, PostgRESTxn additionally verifies the token's <code>aud</code> claim against it.</p>"},{"location":"authentication/static-secret/#rotating-the-key","title":"Rotating the key","text":"<p>Manual process followed by reconfiguring the secrets. For zero-downtime rotation, move to JWKS - the IdP can publish multiple keys (old + new) and clients re-fetch as needed.</p>"},{"location":"concepts/filters/","title":"Filter language","text":"<p>Filters express the <code>where</code> clause of an update, delete, or select op. PostgRESTxn uses a structured JSON grammar with operator-maps, identical in shape to filters from MongoDB, Prisma, Sequelize, and Drizzle.</p>"},{"location":"concepts/filters/#anatomy","title":"Anatomy","text":"<p>A filter is an object of column entries. Each column entry is itself an operator-map - a non-empty object of <code>{operator: value}</code> pairs.</p> <pre><code>\"where\": {\n  \"age\": { \"gte\": 18, \"lte\": 65 },\n  \"status\": { \"in\": [\"active\", \"pending\"] }\n}\n</code></pre> <p>Translates to:</p> <pre><code>WHERE \"age\" &gt;= $1 AND \"age\" &lt;= $2\n  AND \"status\" IN ($3, $4)\n</code></pre> <p>Note</p> <p>The language doesn't support <code>OR</code> operators yet. All operators are <code>AND</code>'ed when resolving.</p>"},{"location":"concepts/filters/#operator-catalog","title":"Operator catalog","text":"Operator Value type SQL Example <code>eq</code> scalar <code>\"col\" = $N</code> <code>{\"id\": {\"eq\": 5}}</code> <code>neq</code> scalar <code>\"col\" != $N</code> <code>{\"status\": {\"neq\": \"deleted\"}}</code> <code>lt</code> scalar <code>\"col\" &lt; $N</code> <code>{\"age\": {\"lt\": 18}}</code> <code>lte</code> scalar <code>\"col\" &lt;= $N</code> <code>{\"age\": {\"lte\": 65}}</code> <code>gt</code> scalar <code>\"col\" &gt; $N</code> <code>{\"score\": {\"gt\": 100}}</code> <code>gte</code> scalar <code>\"col\" &gt;= $N</code> <code>{\"score\": {\"gte\": 80}}</code> <code>like</code> string <code>\"col\" LIKE $N</code> <code>{\"email\": {\"like\": \"%@example.com\"}}</code> <code>ilike</code> string <code>\"col\" ILIKE $N</code> <code>{\"name\": {\"ilike\": \"alice%\"}}</code> <code>is</code> <code>null</code> / <code>true</code> / <code>false</code> <code>\"col\" IS NULL/TRUE/FALSE</code> <code>{\"deleted_at\": {\"is\": null}}</code> <code>in</code> non-empty array <code>\"col\" IN ($N1, ...)</code> <code>{\"id\": {\"in\": [1, 2, 3]}}</code> <code>nin</code> non-empty array <code>\"col\" NOT IN ($N1, ...)</code> <code>{\"id\": {\"nin\": [4, 5]}}</code> <code>not</code> another operator-map <code>NOT (&lt;recursive&gt;)</code> <code>{\"id\": {\"not\": {\"in\": [1, 2]}}}</code> <p>A few notes on specific operators:</p> <ul> <li><code>like</code> / <code>ilike</code> use standard SQL wildcards in the value: <code>%</code> for any sequence, <code>_</code> for a single character.</li> <li><code>not</code> recurses. It wraps another operator-map and negates the result.</li> </ul>"},{"location":"concepts/filters/#multi-operator-examples","title":"Multi-operator examples","text":"<p>Range query (one column, two operators):</p> <pre><code>{\"age\": {\"gte\": 18, \"lte\": 65}}\n// \u2192 \"age\" &gt;= 18 AND \"age\" &lt;= 65\n</code></pre> <p>Pattern + null check (multiple columns):</p> <pre><code>{\"email\": {\"ilike\": \"%@example.com\"}, \"deleted_at\": {\"is\": null}}\n// \u2192 \"email\" ILIKE '%@example.com' AND \"deleted_at\" IS NULL\n</code></pre>"},{"location":"concepts/filters/#cross-op-references-in-filters","title":"Cross-op references in filters","text":"<p>Operator values can be cross-op references. They resolve before the filter is built:</p> <pre><code>{\"user_id\": {\"eq\": \"$create_user.0.id\"}}\n</code></pre> <p>References work inside <code>in</code> / <code>nin</code> arrays too:</p> <pre><code>{\"id\": {\"in\": [\"$a.0.id\", \"$b.0.id\"]}}\n</code></pre>"},{"location":"concepts/filters/#forbidden-shapes","title":"Forbidden shapes","text":"<p>These are rejected with a <code>validation_error</code> (HTTP 422):</p> <ul> <li>Empty operator-map: <code>{\"id\": {}}</code> - a column entry must have at least one operator.</li> <li>Empty list: <code>{\"id\": {\"in\": []}}</code> - <code>in</code> / <code>nin</code> require at least one element.</li> <li>Unknown operator: <code>{\"id\": {\"foobar\": 5}}</code> - operator must be in the catalog.</li> <li>Wrong value type: <code>{\"id\": {\"eq\": [1, 2]}}</code> - <code>eq</code> requires a scalar, not an array.</li> <li><code>is</code> with non-bool/non-null: <code>{\"id\": {\"is\": \"yes\"}}</code> - only JSON <code>null</code> / <code>true</code> / <code>false</code>.</li> </ul>"},{"location":"concepts/operations/","title":"Operations","text":"<p>Each PostgRESTxn request is a JSON array of operations - each one a single CRUD action against a table. Operations within a request execute in order in one atomic transaction.</p>"},{"location":"concepts/operations/#op-types","title":"Op types","text":"<p>PostgRESTxn supports four op types, mapped 1:1 to their SQL equivalents:</p> <code>op</code> SQL What it does <code>insert</code> <code>INSERT</code> Insert one or more rows <code>update</code> <code>UPDATE</code> Update rows matching a filter <code>delete</code> <code>DELETE</code> Delete rows matching a filter <code>select</code> <code>SELECT</code> Read rows <p>There is no <code>rpc</code>, <code>call</code>, or stored-procedure equivalent as of yet.</p>"},{"location":"concepts/operations/#common-fields","title":"Common fields","text":"<p>Every op carries the same three top-level fields:</p> Field Required Description <code>id</code> yes Unique identifier for the op within this request. Can be made of letters, digits, and underscores (cannot start with digits). Used as the key in the response and as the target of cross-op references. <code>op</code> yes One of <code>insert</code>, <code>update</code>, <code>delete</code>, <code>select</code>. <code>table</code> yes Table or view name. Schema-qualified names like <code>auth.users</code> are supported. <p>Beyond these, each op type has its own type-specific fields described below.</p>"},{"location":"concepts/operations/#insert","title":"insert","text":"<p>Adds one or more rows to a table.</p> <pre><code>{\n  \"id\": \"create\",\n  \"op\": \"insert\",\n  \"table\": \"users\",\n  \"values\": [{\"email\": \"alice@example.com\"}, {\"email\": \"bob@example.com\"}]\n}\n</code></pre> <ul> <li><code>values</code> (required): a non-empty array of objects. All objects must have the same keys - each key becomes a column, each object becomes a row.</li> <li>Result: an array of the inserted rows, with auto-generated columns (<code>SERIAL</code> ids, default-valued columns) populated.</li> </ul>"},{"location":"concepts/operations/#update","title":"update","text":"<p>Updates rows matching a filter.</p> <pre><code>{\n  \"id\": \"promote\",\n  \"op\": \"update\",\n  \"table\": \"users\",\n  \"set\": {\"role\": \"admin\"},\n  \"where\": {\"id\": {\"eq\": 42}}\n}\n</code></pre> <ul> <li><code>set</code> (required): a non-empty object mapping column names to new values.</li> <li><code>where</code> (required): the filter predicate. See Filter language.</li> <li>Result: an array of the affected rows, after the update.</li> </ul>"},{"location":"concepts/operations/#delete","title":"delete","text":"<p>Removes rows matching a filter.</p> <pre><code>{\n  \"id\": \"cleanup\",\n  \"op\": \"delete\",\n  \"table\": \"users\",\n  \"where\": {\"id\": {\"eq\": 42}}\n}\n</code></pre> <ul> <li><code>where</code> (required): the filter predicate. See Filter language.</li> <li>Result: an array of the deleted rows.</li> </ul>"},{"location":"concepts/operations/#select","title":"select","text":"<p>Reads rows.</p> <pre><code>{\n  \"id\": \"fetch\",\n  \"op\": \"select\",\n  \"table\": \"users\",\n  \"where\": {\"role\": {\"eq\": \"admin\"}}\n}\n</code></pre> <ul> <li><code>where</code> (optional): the filter predicate. Omit it to select everything (subject to RLS).</li> <li>Result: an array of rows.</li> </ul>"},{"location":"concepts/operations/#result-shape","title":"Result shape","text":"<p>Every op returns a JSON array.</p> <p>For example, the <code>insert</code> op from earlier:</p> <pre><code>{\n  \"id\": \"create\",\n  \"op\": \"insert\",\n  \"table\": \"users\",\n  \"values\": [{\"email\": \"alice@example.com\"}, {\"email\": \"bob@example.com\"}]\n}\n</code></pre> <p>Returns:</p> <pre><code>{\n  \"error\": null,\n  \"data\": {\n    \"create\": [\n      {\"id\": 1, \"email\": \"alice@example.com\"},\n      {\"id\": 2, \"email\": \"bob@example.com\"}\n    ]\n  }\n}\n</code></pre> <p>The <code>data</code> map is keyed by op id, each entry holds the array of affected rows for that op.</p> <p>Zero matches on <code>update</code> / <code>delete</code> / <code>select</code> returns an empty array.</p>"},{"location":"concepts/operations/#atomicity","title":"Atomicity","text":"<p>All ops in a request run in a single Postgres transaction. The first failure rolls back everything.</p>"},{"location":"concepts/operations/#schema-qualification","title":"Schema qualification","text":"<p>Table names may be schema-qualified:</p> <pre><code>{\"table\": \"auth.users\"}\n</code></pre> <p>Each segment must be a valid identifier (<code>\"auth\".\"users\"</code>).</p> <p>PostgRESTxn doesn't allow you to touch schemas not specified in the <code>DB_SCHEMAS</code> allowlist. See Configuration for how to configure this.</p> <p>Attempting to use a schema outside the allowlist returns a <code>validation_error</code> (HTTP 403) with code <code>schema_forbidden</code> for the op:</p> <pre><code>{\n  \"error\": \"validation_error\",\n  \"data\": {\n    \"x\": [{\"code\": \"schema_forbidden\", \"detail\": \"schema \\\"secret\\\" is not allowed\"}]\n  }\n}\n</code></pre>"},{"location":"concepts/references/","title":"Cross-op references","text":"<p>Within a single request, an op can reference the results of any earlier op. This is how you chain operations, inserting a user and then using their newly-generated id in a subsequent insert.</p>"},{"location":"concepts/references/#grammar","title":"Grammar","text":"<p>A reference is a string that starts with <code>$</code>, followed by an op id, optionally followed by <code>.&lt;index&gt;</code> and <code>.&lt;field&gt;</code> segments.</p> <pre><code>$&lt;op_id&gt;\n$&lt;op_id&gt;.&lt;field&gt;\n$&lt;op_id&gt;.&lt;index&gt;.&lt;field&gt;\n</code></pre> Form Resolves to <code>$create_user.id</code> the <code>id</code> field of the first row in <code>create_user</code>'s result <code>$create_user.0.id</code> the <code>id</code> field of the row at index 0 (explicit) <code>$create_user.0</code> the whole first row (valid where a JSON object fits, e.g., writing to a <code>jsonb</code> column) <code>$create_user</code> the whole result array <p>References are valid in:</p> <ul> <li>Whole values inside <code>values</code> or <code>set</code>: <code>{\"user_id\": \"$create_user.0.id\"}</code></li> <li>The value of a filter operator: <code>{\"user_id\": {\"eq\": \"$create_user.0.id\"}}</code></li> <li>List members inside <code>in</code> / <code>nin</code>: <code>{\"id\": {\"in\": [\"$a.0.id\", \"$b.0.id\"]}}</code></li> </ul>"},{"location":"concepts/references/#resolution-rules","title":"Resolution rules","text":"<p>Op results are always arrays (see Operations). So references that don't specify an index implicitly target index 0:</p> Form Use when <code>$op.field</code> shorthand for <code>$op.0.field</code> - the common case <code>$op.0.field</code> explicit equivalent <code>$op.1.field</code>, <code>$op.2.field</code>, ... targeting a specific row in a multi-row result <code>$op</code> only when a JSON-encoded array is acceptable (rare - e.g., writing the whole result to a <code>jsonb</code> column)"},{"location":"concepts/references/#validation-errors","title":"Validation errors","text":"<p>Any value starting with <code>$</code> opts into the reference grammar. If the grammar can't resolve the value, the validator rejects the request with a <code>validation_error</code> (HTTP 422):</p> Code When it fires <code>ref_unknown</code> The reference points at an op id that doesn't exist in the request, or appears later in the array (forward refs aren't allowed). <code>ref_malformed</code> The value starts with <code>$</code> but the grammar can't parse it. e.g., bare <code>$</code>, <code>$.field</code> (empty op id), or <code>$.</code>. <p>To opt-out and use a <code>$</code>-prefixed value as data (rather than as a ref), escape with <code>$$</code>:</p> <pre><code>{\"template\": \"$$create_user.0.id\"}\n// \u2192 stored as: \"$create_user.0.id\"\n</code></pre>"},{"location":"concepts/references/#worked-example","title":"Worked example","text":"<pre><code>[\n  {\n    \"id\": \"create_user\",\n    \"op\": \"insert\",\n    \"table\": \"users\",\n    \"values\": [{\"email\": \"alice@example.com\"}]\n  },\n  {\n    \"id\": \"create_order\",\n    \"op\": \"insert\",\n    \"table\": \"orders\",\n    \"values\": [{\"user_id\": \"$create_user.0.id\", \"status\": \"pending\"}]\n  },\n  {\n    \"id\": \"fetch_order\",\n    \"op\": \"select\",\n    \"table\": \"orders\",\n    \"where\": {\"id\": {\"eq\": \"$create_order.0.id\"}}\n  }\n]\n</code></pre> <p>In sequence:</p> <ol> <li><code>create_user</code> inserts Alice. Postgres assigns <code>id = 1</code>. Result: <code>[{\"id\": 1, \"email\": \"alice@example.com\"}]</code>.</li> <li><code>create_order</code>'s <code>values</code> references <code>$create_user.0.id</code> \u2192 resolves to <code>1</code>. Inserts the order with <code>user_id = 1</code>. Postgres assigns <code>id = 7</code>. Result: <code>[{\"id\": 7, \"user_id\": 1, \"status\": \"pending\"}]</code>.</li> <li><code>fetch_order</code>'s <code>where</code> references <code>$create_order.0.id</code> \u2192 resolves to <code>7</code>. Returns the order.</li> </ol> <p>All three execute in one transaction.</p>"}]}